Perceptron p;

//setting up the sketch
void setup(){
  size(400,400);
  p = new Perceptron();
  println("weight0-" + p.weights[0]);
  println("weight1-" + p.weights
}

//for drawing
void draw(){
  
  //r, g, b (0...255)
  background
}
